import React, { Component } from 'react';
import { Platform, StyleSheet, Text, View } from 'react-native';

type Props = {};

export default class Plane_strip extends Component<Props> {
    render() {
        return (
            <View>
            </View>
        );
    }
}
